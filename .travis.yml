language: go
sudo: false

install:
  - go get "github.com/stretchr/testify/assert"

script:
  - GOPATH=$TRAVIS_BUILD_DIR
  - go test arithmetic -json > testdata.json

after_script:
  - python <(curl -s https://raw.githubusercontent.com/report-ci/scripts/master/upload.py)